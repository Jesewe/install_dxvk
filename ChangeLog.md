## Summary

- Refactored the installation logic into separate methods for removing old DLLs (`_remove_existing_dlls`) and installing new ones (`_install_dlls`).
- Updated the Python version in the build workflow to 3.12.10.